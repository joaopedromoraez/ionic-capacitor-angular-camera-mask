import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-capacitor-angular-camera-mask',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
